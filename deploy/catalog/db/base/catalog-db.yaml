apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: catalog-db
  namespace: catalog
spec:
  instances: 1
  storage:
    size: 1Gi
  bootstrap:
    initdb:
      database: catalog
      owner: catalog
