apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "reservations.fullname" . }}-migrate
  labels:
    {{- include "reservations.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-5"
    "helm.sh/hook-delete-policy": before-hook-creation
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        {{- include "reservations.selectorLabels" . | nindent 8 }}
    spec:
      restartPolicy: Never
      containers:
        - name: migrate
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args: ["migrate"]
          env:
            {{- include "reservations.dbEnv" . | nindent 12 }}
            {{- include "reservations.rabbitEnv" . | nindent 12 }}
