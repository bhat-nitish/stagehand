apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: reservations-db
  namespace: reservations
spec:
  instances: 1
  storage:
    size: 1Gi
  bootstrap:
    initdb:
      database: reservations
      owner: reservations
