# Dev credentials, same posture as Keycloak's admin/admin in this repo.
# Prod replaces this with a generated secret outside version control.
apiVersion: v1
kind: Secret
metadata:
  name: rabbitmq-credentials
  namespace: messaging
type: Opaque
stringData:
  username: stagehand
  password: dev-rabbit-password
